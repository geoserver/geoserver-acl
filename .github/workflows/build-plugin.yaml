# Builds and pushes docker images on main and tags
name: Test GeoSever plugin
on:
  push:
    branches:
      - '**'
  pull_request:
    branches: [ main ]

jobs:
  build-plugin:
    name: Test Plugin for GeoSever
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        gs-version: [2.22.2, 2.23.0]
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup Java
      uses: actions/setup-java@v2
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'maven'
    - name: Test plugin
      run: |
        ./mvnw -Dgs.version=${{ matrix.gs-version }} verify -pl :gs-acl-client-plugin -am -ntp
